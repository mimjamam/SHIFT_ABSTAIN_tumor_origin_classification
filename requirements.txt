# Phase 0-1: data acquisition and feature extraction
numpy
pandas
pyarrow
py2bit
snakemake

# Phase 2: baseline models
scikit-learn

# Phase 3: uncertainty (deep ensemble + MC-dropout MLP)
# CPU-only build: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch

# Phase 4: evaluation figures
matplotlib

# Phase 5: API + UI
fastapi
uvicorn
python-multipart
gradio
